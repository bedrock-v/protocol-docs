# SystemDiagnosticTimingInfo

`struct`

```mermaid
flowchart LR
  ROOT(["SystemDiagnosticTimingInfo"])
  ROOT -->|"Display Name"| string["string"]
  ROOT -->|"System Index"| uint64["uint64"]
  ROOT -->|"Time in NS"| uint64["uint64"]
  ROOT -->|"Percent of Total"| uint8["uint8"]
```

