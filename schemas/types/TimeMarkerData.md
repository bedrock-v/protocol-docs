# TimeMarkerData

`struct`

```mermaid
flowchart LR
  ROOT(["TimeMarkerData"])
  ROOT -->|"Id"| uint64["uint64"]
  ROOT -->|"Name"| string["string"]
  ROOT -->|"Time"| int32["int32"]
  ROOT -->|"Period"| int32["int32"]
```

