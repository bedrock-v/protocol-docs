# ItemStackRequestCraftLoomAction

`struct`

```mermaid
flowchart LR
  ROOT(["ItemStackRequestCraftLoomAction"])
  ROOT -->|"Action type"| ItemStackRequestActionType["ItemStackRequestActionType"]
  ROOT -->|"Pattern Name Id"| string["string"]
  ROOT -->|"Num Crafts"| uint8["uint8"]
```

