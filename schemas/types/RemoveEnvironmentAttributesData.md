# RemoveEnvironmentAttributesData

`struct`

```mermaid
flowchart LR
  ROOT(["RemoveEnvironmentAttributesData"])
  ROOT -->|"Attribute Layer Name"| string["string"]
  ROOT -->|"Attribute Layer Dimension"| DimensionType["DimensionType"]
  ROOT -->|"Attributes"| string["string[]"]
```

