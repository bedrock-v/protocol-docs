# Bedrock::Safety::RedactableString

`struct`

```mermaid
flowchart LR
  ROOT(["Bedrock::Safety::RedactableString"])
  ROOT -->|"Unredacted"| string["string"]
  ROOT -->|"Redacted"| string["string"]
```

