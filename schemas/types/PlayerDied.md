# PlayerDied

`struct`

```mermaid
flowchart LR
  ROOT(["PlayerDied"])
  ROOT -->|"Instigator Actor ID"| int32["int32"]
  ROOT -->|"Instigator Mob Variant"| int32["int32"]
  ROOT -->|"Damage Source"| int32["int32"]
  ROOT -->|"Died in Raid?"| boolean["boolean"]
```

