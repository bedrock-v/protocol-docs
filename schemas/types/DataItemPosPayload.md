# DataItemPosPayload

`struct`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart LR
  ROOT(["DataItemPosPayload"])
  ROOT -->|"Type"| DataItemType["DataItemType"]
  ROOT -->|"Value"| BlockPos["BlockPos"]
```

