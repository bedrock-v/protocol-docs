# CameraInstruction::FadeInstruction::ColorOption

`struct`

```mermaid
flowchart LR
  ROOT(["CameraInstruction::FadeInstruction::ColorOption"])
  ROOT -->|"Red"| float["float"]
  ROOT -->|"Green"| float["float"]
  ROOT -->|"Blue"| float["float"]
```

