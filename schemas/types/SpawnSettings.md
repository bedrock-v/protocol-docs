# SpawnSettings

`struct`

```mermaid
flowchart LR
  ROOT(["SpawnSettings"])
  ROOT -->|"Spawn Biome Type"| SpawnBiomeType["SpawnBiomeType"]
  ROOT -->|"User Defined Biome Name"| string["string"]
  ROOT -->|"Dimension"| int32["int32"]
```

