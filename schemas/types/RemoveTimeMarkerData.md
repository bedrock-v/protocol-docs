# RemoveTimeMarkerData

`struct`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart LR
  ROOT(["RemoveTimeMarkerData"])
  ROOT -->|"ClockId"| uint64["uint64"]
  ROOT -->|"TimeMarkerIds"| uint64["uint64[]"]
```

