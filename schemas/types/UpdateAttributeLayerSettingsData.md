# UpdateAttributeLayerSettingsData

`struct`

```mermaid
flowchart LR
  ROOT(["UpdateAttributeLayerSettingsData"])
  ROOT -->|"Attribute Layer Name"| string["string"]
  ROOT -->|"Attribute Layer Dimension"| DimensionType["DimensionType"]
  ROOT -->|"Attributes Layer Settings"| AttributeLayerSettings["AttributeLayerSettings"]
```

