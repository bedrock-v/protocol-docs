# NewInteractionModel

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["NewInteractionModel"])
  ROOT --> Touch["Touch"]
  ROOT --> Crosshair["Crosshair"]
  ROOT --> Classic["Classic"]
  ROOT --> Count["Count"]
```

