# BiomeTemperatureCategory

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["BiomeTemperatureCategory"])
  ROOT --> Medium["Medium"]
  ROOT --> Warm["Warm"]
  ROOT --> Lukewarm["Lukewarm"]
  ROOT --> Cold["Cold"]
  ROOT --> Frozen["Frozen"]
```

