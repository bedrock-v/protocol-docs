# ClientPlayMode

`enum` - wire `uint32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ClientPlayMode"])
  ROOT --> Normal["Normal"]
  ROOT --> Teaser["Teaser"]
  ROOT --> Screen["Screen"]
  ROOT --> ExitLevel["ExitLevel"]
  ROOT --> NumModes["NumModes"]
```

