# DataDrivenScreenClosedReason

`enum` - wire `uint8`

```mermaid
flowchart TB
  ROOT(["DataDrivenScreenClosedReason"])
  ROOT --> ProgrammaticClose["ProgrammaticClose"]
  ROOT --> ProgrammaticCloseAll["ProgrammaticCloseAll"]
  ROOT --> ClientCanceled["ClientCanceled"]
  ROOT --> UserBusy["UserBusy"]
  ROOT --> InvalidForm["InvalidForm"]
```

