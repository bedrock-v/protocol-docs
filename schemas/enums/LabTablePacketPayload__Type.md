# LabTablePacketPayload::Type

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["LabTablePacketPayload::Type"])
  ROOT --> StartCombine["StartCombine"]
  ROOT --> StartReaction["StartReaction"]
  ROOT --> Reset["Reset"]
```

