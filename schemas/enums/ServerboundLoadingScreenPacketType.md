# ServerboundLoadingScreenPacketType

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ServerboundLoadingScreenPacketType"])
  ROOT --> Unknown["Unknown"]
  ROOT --> StartLoadingScreen["StartLoadingScreen"]
  ROOT --> EndLoadingScreen["EndLoadingScreen"]
```

