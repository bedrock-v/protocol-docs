# SimulationType

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["SimulationType"])
  ROOT --> Game["Game"]
  ROOT --> Editor["Editor"]
  ROOT --> Test["Test"]
  ROOT --> INVALID["INVALID"]
```

