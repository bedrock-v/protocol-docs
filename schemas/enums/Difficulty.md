# Difficulty

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["Difficulty"])
  ROOT --> Peaceful["Peaceful"]
  ROOT --> Easy["Easy"]
  ROOT --> Normal["Normal"]
  ROOT --> Hard["Hard"]
  ROOT --> Count["Count"]
  ROOT --> Unknown["Unknown"]
```

