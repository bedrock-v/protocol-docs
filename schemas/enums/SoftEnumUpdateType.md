# SoftEnumUpdateType

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["SoftEnumUpdateType"])
  ROOT --> Add["Add"]
  ROOT --> Remove["Remove"]
  ROOT --> Replace["Replace"]
```

