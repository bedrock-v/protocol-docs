# FloatAttributeOperation

`enum` - wire `int32`

```mermaid
flowchart TB
  ROOT(["FloatAttributeOperation"])
  ROOT --> OVERRIDE["OVERRIDE"]
  ROOT --> ALPHA_BLEND["ALPHA_BLEND"]
  ROOT --> ADD["ADD"]
  ROOT --> SUBTRACT["SUBTRACT"]
  ROOT --> MULTIPLY["MULTIPLY"]
  ROOT --> MINIMUM["MINIMUM"]
  ROOT --> MAXIMUM["MAXIMUM"]
```

