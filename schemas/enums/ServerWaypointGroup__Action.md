# ServerWaypointGroup::Action

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ServerWaypointGroup::Action"])
  ROOT --> None["None"]
  ROOT --> Add["Add"]
  ROOT --> Remove["Remove"]
  ROOT --> Update["Update"]
```

