# RandomDistributionType

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["RandomDistributionType"])
  ROOT --> SingleValued["SingleValued"]
  ROOT --> Uniform["Uniform"]
  ROOT --> Gaussian["Gaussian"]
  ROOT --> InverseGaussian["InverseGaussian"]
  ROOT --> FixedGrid["FixedGrid"]
  ROOT --> JitteredGrid["JitteredGrid"]
  ROOT --> Triangle["Triangle"]
```

