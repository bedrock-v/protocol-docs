# ServerEditorConnectionPolicy

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ServerEditorConnectionPolicy"])
  ROOT --> MatchWorldType["MatchWorldType"]
  ROOT --> EditorOnly["EditorOnly"]
  ROOT --> VanillaOnly["VanillaOnly"]
  ROOT --> Mixed["Mixed"]
```

