# MobEffectPacketPayload::Event

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["MobEffectPacketPayload::Event"])
  ROOT --> Invalid["Invalid"]
  ROOT --> Add["Add"]
  ROOT --> Update["Update"]
  ROOT --> Remove["Remove"]
```

