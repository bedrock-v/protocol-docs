# PacketCompressionAlgorithm

`enum` - wire `uint16`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["PacketCompressionAlgorithm"])
  ROOT --> ZLib["ZLib"]
  ROOT --> Snappy["Snappy"]
  ROOT --> None["None"]
```

