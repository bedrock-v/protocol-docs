# PositionTrackingDBServerBroadcastPacketPayload::Action

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["PositionTrackingDBServerBroadcastPacketPayload::Action"])
  ROOT --> Update["Update"]
  ROOT --> Destroy["Destroy"]
  ROOT --> NotFound["NotFound"]
```

