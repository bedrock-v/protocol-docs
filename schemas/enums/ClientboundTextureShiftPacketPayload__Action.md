# ClientboundTextureShiftPacketPayload::Action

`enum` - wire `uint8`

```mermaid
flowchart TB
  ROOT(["ClientboundTextureShiftPacketPayload::Action"])
  ROOT --> Invalid["Invalid"]
  ROOT --> Initialize["Initialize"]
  ROOT --> Start["Start"]
  ROOT --> SetEnabled["SetEnabled"]
  ROOT --> Sync["Sync"]
```

