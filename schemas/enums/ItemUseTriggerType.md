# ItemUseTriggerType

`enum` - wire `uint8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ItemUseTriggerType"])
  ROOT --> Unknown["Unknown"]
  ROOT --> Player_Input["Player Input"]
  ROOT --> Simulation_Tick["Simulation Tick"]
```

