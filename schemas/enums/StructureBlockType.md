# StructureBlockType

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["StructureBlockType"])
  ROOT --> Data["Data"]
  ROOT --> Save["Save"]
  ROOT --> Load["Load"]
  ROOT --> Corner["Corner"]
  ROOT --> Invalid["Invalid"]
  ROOT --> Export["Export"]
```

