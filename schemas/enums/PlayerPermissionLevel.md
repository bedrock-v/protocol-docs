# PlayerPermissionLevel

`enum` - wire `int8`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["PlayerPermissionLevel"])
  ROOT --> Visitor["Visitor"]
  ROOT --> Member["Member"]
  ROOT --> Operator["Operator"]
  ROOT --> Custom["Custom"]
```

