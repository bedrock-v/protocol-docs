# ShowCreditsPacketPayload::CreditsState

`enum` - wire `int32`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["ShowCreditsPacketPayload::CreditsState"])
  ROOT --> Start["Start"]
  ROOT --> Finished["Finished"]
```

