# PackType

`enum` - wire `object`

- protocol: 2168
- minecraft: 1.26.40

```mermaid
flowchart TB
  ROOT(["PackType"])
  ROOT --> Invalid["Invalid"]
  ROOT --> Addon["Addon"]
  ROOT --> Cached["Cached"]
  ROOT --> CopyProtected["CopyProtected"]
  ROOT --> Behavior["Behavior"]
  ROOT --> PersonaPiece["PersonaPiece"]
  ROOT --> Resources["Resources"]
  ROOT --> Skins["Skins"]
  ROOT --> WorldTemplate["WorldTemplate"]
```

