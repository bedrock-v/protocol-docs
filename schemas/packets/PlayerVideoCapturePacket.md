# PlayerVideoCapturePacket

`packet` - id **324**

Internal

```mermaid
flowchart LR
  ROOT(["PlayerVideoCapturePacket"])
  ROOT -->|"Start Video Capture"| unknown["unknown"]
  ROOT -->|"Stop Video Capture"| unknown["unknown"]
```

