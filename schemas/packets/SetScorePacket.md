# SetScorePacket

`packet` - id **108**

Sets the scoreboard which is used for 3rd party content.

```mermaid
flowchart LR
  ROOT(["SetScorePacket"])
  ROOT -->|"Score Info"| unknown["unknown[]"]
```

