# ServerSettingsRequestPacket

`packet` - id **102**

It is also an empty packet. There is no handler for this packet, should be removed.

```mermaid
flowchart LR
  ROOT(["ServerSettingsRequestPacket"])
```

