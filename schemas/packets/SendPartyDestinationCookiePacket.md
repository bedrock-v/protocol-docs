# SendPartyDestinationCookiePacket

`packet` - id **349**



```mermaid
flowchart LR
  ROOT(["SendPartyDestinationCookiePacket"])
  ROOT -->|"cookie"| string["string"]
  ROOT -->|"intent"| PartyDestinationCookieIntent["PartyDestinationCookieIntent"]
  ROOT -->|"destination_name"| string["string"]
```

