# SetPlayerInventoryOptionsPacket

`packet` - id **307**



```mermaid
flowchart LR
  ROOT(["SetPlayerInventoryOptionsPacket"])
  ROOT -->|"Inventory Options"| InventoryOptions["InventoryOptions"]
```

