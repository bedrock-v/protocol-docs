# ClientboundCloseFormPacket

`packet` - id **310**

- protocol: 2168
- minecraft: 1.26.40

This will only have an affect if the client currently has a server form on the UI stack.

```mermaid
flowchart LR
  ROOT(["ClientboundCloseFormPacket"])
```

